resource "aws_key_pair" "test-ec2-key" {
  key_name   = "testkey"
  public_key = file(var.PUB_KEY)
}

resource "aws_instance" "test-ec2-web" {
  ami                    = var.AMIS[var.REGION]
  instance_type          = "t2.micro"
  subnet_id              = aws_subnet.test-sub-pub-1.id
  key_name               = aws_key_pair.test-ec2-key.key_name
  vpc_security_group_ids = [aws_security_group.test_vpc_sg.id]
  tags = {
    Name = "my-test-server"
  }
}

resource "aws_ebs_volume" "vol_4_test" {
  availability_zone = var.ZONE1
  size              = 3
  tags = {
    Name = "extr-vol-4-for-test"
  }
}

resource "aws_volume_attachment" "atch_vol_tes" {
  device_name = "/dev/xvdh"
  volume_id   = aws_ebs_volume.vol_4_test.id
  instance_id = aws_instance.test-ec2-web.id
}

output "PublicIP" {
  value = aws_instance.test-ec2-web.public_ip
}

